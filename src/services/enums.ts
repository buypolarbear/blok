const enum DIRECTION {
  right,
  left
}

const enum TICKER {
  BTC,
  LTC,
  ETH,
  XRP,
  DASH,
  STEEM
}

const enum SIZE {
  small,
  big,
  normal
}

const enum COLOR {
  white,
  grey,
  green,
  blue,
  red
}

export { DIRECTION, TICKER, SIZE, COLOR };
